# incident-management-system
A scalable, asynchronous Incident Management System designed to handle high-volume failure signals from distributed systems (APIs, caches, queues, databases). The system ingests up to 10,000 signals/sec, applies debouncing logic to group related events into single incidents, and stores both raw and structured data across optimized storage layers.
